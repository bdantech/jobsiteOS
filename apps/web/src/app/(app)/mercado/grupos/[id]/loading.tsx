import { GrupoCarregando } from '@/components/mercado/grupos/grupo-detalhe'

/** Exibido enquanto a guarda RSC (sessão + módulo) resolve, antes do client montar. */
export default function CarregandoGrupo() {
  return <GrupoCarregando />
}
